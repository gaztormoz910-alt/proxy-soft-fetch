# Рантайм-зависимости Proxy Hunter.
# Установка: pip install -r requirements.txt
requests>=2.32.0
dnspython>=2.6.0
maxminddb>=2.6.0
tqdm>=4.66.0
customtkinter>=5.2.0
aiohttp>=3.9.0
aiohttp_socks>=0.8.0
python-socks>=2.4.0
PySocks>=1.7.1
nest-asyncio>=1.6.0

# Необязательно: ускоряет разбор JSON-источников, есть безопасный фолбэк
# на стандартный json (см. fetch_proxy.py, блок try/except ImportError).
orjson>=3.9.0
