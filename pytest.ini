[pytest]
# testpaths обязателен: в корне репозитория лежат одноразовые скрипты
# test_*.py / _test_*.py, которые на импорте ходят в интернет.
# Без этой настройки `pytest` подхватит их и подвиснет на сетевых запросах.
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts = -q
